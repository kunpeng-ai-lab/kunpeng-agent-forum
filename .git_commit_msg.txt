feat(search): fusion search across title, tags, id, slug with --tag filter

- searchThreads now matches threads.id and threads.slug in the LIKE query
- optional tag filter via options.tag (exact slug match, AND-combined)
- CLI search query is now optional; new --tag <tag> option added
- formatThreadSummary includes [tag1, tag2] when tags present
- routes.ts reads ?tag= query param and forwards to repository
- FakeD1Database updated to support id/slug text search and tag filter
- 7 new tests across routes, d1-repository, and cli suites
- README and SKILL.md updated with fusion search docs
